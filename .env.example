# 复制本文件为 .env 后填写，切勿提交 .env 到 Git。
# Windows：文件名必须是 .env（若显示为 .env.txt 则无效）；记事本另存为「UTF-8」编码。
# 必须在项目根目录（与 server.js 同目录），且用 npm start 会加载该文件。

# —— DeepSeek（推荐，国内可用）任选其一写法 ——
# 写法 1：只填 DeepSeek Key（等号右侧粘贴密钥，不要加引号）
DEEPSEEK_API_KEY=

# 写法 2：使用 OpenAI 兼容变量名（与 DeepSeek 官方文档一致）
# OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.deepseek.com/v1
# OPENAI_MODEL=deepseek-chat

# 写法 3：已有 OPENAI_API_KEY 但想用 DeepSeek 端点时可加：
# LLM_PROVIDER=deepseek

# 其它：OpenAI 官方或其它兼容接口自行设置 BASE_URL 与 MODEL
# OPENAI_TIMEOUT_MS=60000

# 视觉分析（需支持 image_url 的 OpenAI 兼容端点；DeepSeek 纯文本端点会回退为文字提示）
# VISION_MODEL=gpt-4o-mini

# —— Gemini（多模态与润色，可选）——
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-1.5-pro

# 可选：生产环境建议设置随机长字符串（当前登录态以数据库 token 为准，此变量预留扩展）
# SESSION_SECRET=

# 知识喂养 / 热重载：首页「周末知识喂养」与 POST /api/knowledge/ingest、/api/admin/knowledge/reload 需携带请求头 X-Admin-Key
# ADMIN_KEY=

# 仅本机开发：为 1 时，用 localhost/127.0.0.1 打开站点会由 /api/meta 自动把密钥同步到浏览器「管理密钥」框（生产环境勿开启）
# ADMIN_CLIENT_SEED=0

# 每周更新 public/data/knowledge.json 后：重启 Node，或调用 POST /api/admin/knowledge/reload（同上密钥）
